import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GasBot — Web3 Gas Tracker & AI Chat",
  description:
    "Real-time Ethereum gas price tracker with AI-powered chat assistant. Get live gas fees, transaction cost estimates, and expert advice powered by Etherscan and Gemini AI.",
  keywords: ["ethereum", "gas tracker", "web3", "gas prices", "gwei", "etherscan", "AI chat"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
